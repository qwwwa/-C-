//编写一段程序，像右面那样读取两个整数，然后显示前者是后者的百分之几
//请输入两个整数。
//整数x：
//整数y：
//x的值是y的64%。
#include<stdio.h>
int main()
{
  int x,y;
	puts("请输入两个整数。");
	printf("整数x:");
	scanf("%d", &x);
	printf("整数y:");
	scanf("%d", &y);
  
  printf("x的值是y的%d%%.\n",x * 100 / y);
  return 0;
}
