//编写一段程序，输入两个整数
//如果差值小于等于10，显示：他们的差小于等于10
//否则，显示：他们的差大于等于11
#include<stdio.h>
int main()
{
	int a, b;

	puts("请输入2个整数。");

	printf("整数A：");
	scanf("%d", &a);
	printf("整数B：");
	scanf("%d", &b);

	if (a - b == 10||a-b<10)
		printf("他们的差小于等于10");
	else
		printf("他们的差大于等于11");

	return 0;
}
