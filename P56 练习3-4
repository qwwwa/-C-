//编写一段程序，像右面这样输入两个整数，如果两数值相等，则显示“A和B相等”。如果A大于B，则显示“A大于B”。如果A小于B，则显示“A小于B”。
//请输入两个整数。
//整数A：54
//整数B：12
//A大于B
#include<stdio.h>
int main()
{
  int A,B;
  puts("请输入两个整数：");
	printf("整数A:");
	scanf("%d", &A);
	printf("整数B:");
	scanf("%d", &B);
  
  if(A==B)
    printf("A和B相等");
  else if(A>B)
    printf("A大于B");
  else
    printf("A小于B");
  return 0;
}
