//读取两个整数并用实数显示他们的值（类型转换）
#include<stdio.h>
int main()
{
  int a,b;
  puts("请输入两个整数。");
  printf("整数a:");
	scanf("%d", &a);
	printf("整数b:");
	scanf("%d", &b);
  
  printf("他们的平均值是%f.\n",(double)(a + b)/2);
  
  return 0;
}
